*Inter Process Communication using pipes*

Given two files it opens or creates them and two commands it searches them from the path and executes both piping the input from cmd1 to cmd2.</br>

### Usage

Build with `make`.</br>

`./pipex [input_file] "program arguments" "program arguments" [output_file]`
